# Copy this file to .env.local and set your password.
# NOTE: NEXT_PUBLIC_ vars are bundled into client JS and visible to anyone who
# inspects the page. The admin only edits your local browser copy, so this is
# convenience, not real security.
NEXT_PUBLIC_ADMIN_PASSWORD=change-me-halek
